const config = {
    db: {
      /* don't expose password or any sensitive info, done only for demo */
      host: "localhost",
      user: "root",
      password: "pizza29032004",
      database: "eventos",
    },
    listPerPage: 10,
  };
  module.exports = config;